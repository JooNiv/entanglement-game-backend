qx_token = "token" # demo token, no token (or invalid token) will use a simulator
slurm_project_id = "project_id" # demo project
device = "demo" #Q50 or Helmi for production
TEST = 0 # 1 true, 0 false