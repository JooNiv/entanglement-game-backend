# demo token, no token (or invalid token) will use a simulator
qx_token = "token"

# demo project
slurm_project_id = "project_id" 

#q50 or helmi for production
device = "demo"

# 1 true, 0 false
TEST = 0

ADMIN_USER = "admin"
ADMIN_PASS = "1234"