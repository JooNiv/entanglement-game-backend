services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: acf-demo-backend
    env_file:
      - ./backend/.env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"