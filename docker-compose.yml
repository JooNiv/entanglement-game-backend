services:
  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: acf-demo-backend
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    ports:
      - "8000:8000"